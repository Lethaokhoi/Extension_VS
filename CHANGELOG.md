# Changelog

## 0.3.0

- **AI:** Tạo `gen.cpp` + `brute.cpp` từ đề bài (OpenAI hoặc API tương thích)
- Đọc đề từ `de.txt`, `problem.md`, file đang mở, hoặc dán tay
- Tùy chọn gửi kèm `main.cpp` cho AI tham khảo

## 0.2.0

- Sinh cặp `.in` / `.out`: `gen.cpp` tạo input, `brute.cpp` (code trâu) tạo đáp án
- Lệnh khởi tạo workspace và sinh test
- Hỗ trợ C++ và Python, cấu hình timeout riêng cho brute
